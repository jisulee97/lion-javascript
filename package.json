{
  "scripts": {
    "start": "live-server client --host=localhost --port=5500 --no-browser",
    "serve": "node server/index.js"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "json-server": "^0.17.3",
    "live-server": "^1.2.2",
    "prettier": "^2.8.8"
  }
}
